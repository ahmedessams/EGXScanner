{
  "name": "07 - EGX Breakout Scanner",
  "active": false,
  "nodes": [
    {
      "name": "Overview",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        0,
        -300
      ],
      "parameters": {
        "content": "## 07 - EGX Breakout Scanner\n\nIndependent 0-100 breakout score (code/breakoutScore.js): proximity to/breach of resistance, RVOL, trend, MACD, RSI, recent-highs confirmation. Classifies BREAKOUT_CONFIRMED / BREAKOUT_WATCH / NO_BREAKOUT.\n\nRequires 04, 05, 06 to have already run for the same asOfDate. Self-sufficient: creates the scanner_run row if it doesn't exist yet (idempotent), so it can be tested standalone or orchestrated by 12-egx-daily-master-workflow.\n\nEnv: BREAKOUT_MIN_RVOL, BREAKOUT_STRONG_RVOL, BREAKOUT_DISTANCE_PCT.",
        "height": 300,
        "width": 480,
        "color": 5
      },
      "id": "68c0a7a3-f9e9-45c6-b42e-50c71042f348"
    },
    {
      "name": "Manual Trigger",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "parameters": {},
      "position": [
        0,
        0
      ],
      "id": "afcf450f-37e6-4d62-9d36-8bb8e5f7ca89"
    },
    {
      "name": "When Executed by Another Workflow",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.1,
      "parameters": {
        "inputSource": "passthrough"
      },
      "position": [
        260,
        0
      ],
      "id": "5ba85bce-af7e-458f-8f0a-fe33321f7d21"
    },
    {
      "name": "Attach As-Of Date",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "// Reading the trigger lookup on a trigger node that never fired THROWS in\n// this n8n version (confirmed via live E2E testing: running this workflow\n// via Manual Trigger alone, with Execute Workflow Trigger never executing,\n// crashed here) — wrap in try/catch so both \"never executed\" and \"executed\n// with zero items\" degrade to the same safe default.\nlet trigger = {};\ntry {\n  const triggerItems = $('When Executed by Another Workflow').all();\n  trigger = triggerItems.length ? triggerItems[0].json : {};\n} catch (e) { trigger = {}; }\nconst asOfDate = trigger.asOfDate || new Date().toISOString().slice(0, 10);\nconst runType = trigger.runType || 'LIVE';\n// Multi-market addition: defaults to EGX so every existing caller that\n// doesn't pass market (an old Execute Workflow node, a bare Manual\n// Trigger run) keeps behaving exactly as before. market doubles as both\n// our own stocks.exchange/scanner_runs.market value AND the EODHD\n// exchange-code suffix (see 01/02/03) — true for EGX and US alike, so no\n// separate markets-table lookup is needed in the daily per-stock chain;\n// only 11's liquidity-threshold lookup needs real per-market config.\nconst market = trigger.market || 'EGX';\nreturn [{ json: { asOfDate, runType, market } }];"
      },
      "position": [
        520,
        0
      ],
      "id": "cc78181e-a37b-444a-b0be-b59725a600d8"
    },
    {
      "name": "Get or Create Scanner Run",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "parameters": {
        "operation": "executeQuery",
        "query": "INSERT INTO scanner_runs (trading_date, run_type, market, status, started_at)\nVALUES ($1, $2, $3, 'RUNNING', now())\nON CONFLICT (trading_date, run_type, market) DO UPDATE SET trading_date = EXCLUDED.trading_date\nRETURNING id, trading_date;",
        "options": {
          "queryReplacement": "={{ [$json.asOfDate, $json.runType, $json.market] }}"
        }
      },
      "executeOnce": true,
      "position": [
        780,
        0
      ],
      "id": "7a3402fb-221c-4276-a926-f84db2a200e7"
    },
    {
      "name": "Load Active Stocks",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT id, symbol FROM stocks WHERE exchange = $1 AND active = TRUE ORDER BY symbol;",
        "options": {
          "queryReplacement": "={{ [$('Attach As-Of Date').first().json.market] }}"
        }
      },
      "executeOnce": true,
      "position": [
        1040,
        0
      ],
      "id": "b6964b33-7dab-42f0-acc0-04dd74fa3de1"
    },
    {
      "name": "Attach Scanner Context",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "const asOfDate = $('Attach As-Of Date').first().json.asOfDate;\nconst scannerRunId = $('Get or Create Scanner Run').first().json.id;\nreturn $input.all().map((item) => ({\n  json: { ...item.json, asOfDate, scannerRunId },\n}));"
      },
      "position": [
        1300,
        0
      ],
      "id": "937d8940-19bb-4d62-a361-2bdcf34b682f"
    },
    {
      "name": "Load Snapshot",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "parameters": {
        "operation": "executeQuery",
        "query": "WITH recent_prices AS (\n  SELECT trading_date, open, high, low, close, volume\n  FROM daily_prices WHERE stock_id = $1 AND trading_date <= $2\n  ORDER BY trading_date DESC LIMIT 25\n),\nrecent_ta AS (\n  SELECT trading_date, ema9, ema20, ema50, ema200, rsi14, macd, macd_signal, macd_histogram,\n         atr14, relative_volume20, high20, high252, low252, distance_52w_high\n  FROM technical_analysis WHERE stock_id = $1 AND trading_date <= $2\n  ORDER BY trading_date DESC LIMIT 25\n),\nsr AS (\n  SELECT support1, support2, support3, resistance1, resistance2, resistance3,\n         support1_strength, nearest_support_distance_pct, nearest_resistance_distance_pct\n  FROM support_resistance WHERE stock_id = $1 AND trading_date = $2\n)\nSELECT\n  (SELECT json_agg(p.* ORDER BY p.trading_date DESC) FROM recent_prices p) AS prices,\n  (SELECT json_agg(t.* ORDER BY t.trading_date DESC) FROM recent_ta t) AS ta,\n  (SELECT row_to_json(sr) FROM sr) AS sr;",
        "options": {
          "queryReplacement": "={{ [$json.id, $json.asOfDate] }}"
        }
      },
      "onError": "continueRegularOutput",
      "position": [
        1560,
        0
      ],
      "id": "725e5cc3-2f02-4967-9c18-75db0e8cf6a7"
    },
    {
      "name": "Calculate Breakout Score",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "function isNumber(v) { return typeof v === 'number' && Number.isFinite(v); }\nfunction pctChangeSafe(from, to) {\n  if (!isNumber(from) || !isNumber(to) || from === 0) return null;\n  return ((to - from) / from) * 100;\n}\n\nfunction deriveFields(row) {\n  const prices = row.prices || [];   // index 0 = most recent (asOfDate)\n  const ta = row.ta || [];           // index 0 = most recent (asOfDate)\n  const sr = row.sr || {};\n\n  const p0 = prices[0] || {};\n  const t0 = ta[0] || {};\n  const t1 = ta[1] || {};\n\n  const closeAt = (n) => (prices[n] ? prices[n].close : null);\n  const close = p0.close;\n\n  const return1d = pctChangeSafe(closeAt(1), close);\n  const return3d = pctChangeSafe(closeAt(3), close);\n  const return5d = pctChangeSafe(closeAt(5), close);\n  const return10d = pctChangeSafe(closeAt(10), close);\n  const return20d = pctChangeSafe(closeAt(20), close);\n\n  const ema50Today = t0.ema50;\n  const ema50FiveAgo = ta[5] ? ta[5].ema50 : null;\n  const ema50Rising = isNumber(ema50Today) && isNumber(ema50FiveAgo) ? ema50Today > ema50FiveAgo : false;\n\n  const macdBullishCrossover = isNumber(t0.macd) && isNumber(t0.macd_signal) && isNumber(t1.macd) && isNumber(t1.macd_signal)\n    ? (t0.macd > t0.macd_signal && t1.macd <= t1.macd_signal)\n    : false;\n  const macdImproving = isNumber(t0.macd_histogram) && isNumber(t1.macd_histogram)\n    ? t0.macd_histogram > t1.macd_histogram\n    : false;\n\n  // Higher-highs/higher-lows: compares the max high / min low of the most\n  // recent 5 sessions against the prior 5 — a simple, honest proxy for swing\n  // structure, not a full fractal re-analysis (that's supportResistance.js's job).\n  const last5 = prices.slice(0, 5);\n  const prior5 = prices.slice(5, 10);\n  let higherHighs = false, higherLows = false;\n  if (last5.length === 5 && prior5.length === 5) {\n    higherHighs = Math.max(...last5.map((c) => c.high)) > Math.max(...prior5.map((c) => c.high));\n    higherLows = Math.min(...last5.map((c) => c.low)) > Math.min(...prior5.map((c) => c.low));\n  }\n\n  // Selling-volume-declining proxy: average volume on DOWN sessions in the\n  // most recent 5 bars vs the prior 5 bars.\n  function avgDownVolume(slice) {\n    const downBars = [];\n    for (let i = 0; i < slice.length - 1; i++) {\n      if (slice[i].close < slice[i + 1].close) downBars.push(slice[i].volume);\n    }\n    if (!downBars.length) return null;\n    return downBars.reduce((a, b) => a + b, 0) / downBars.length;\n  }\n  const recentDownVol = avgDownVolume(last5);\n  const priorDownVol = avgDownVolume(prior5.concat(last5.slice(-1)));\n  const sellVolumeDeclining = isNumber(recentDownVol) && isNumber(priorDownVol) ? recentDownVol < priorDownVol : false;\n\n  const bullishCandle = isNumber(p0.high) && isNumber(p0.low) && p0.high !== p0.low && isNumber(p0.close) && isNumber(p0.open)\n    ? ((p0.close - p0.low) / (p0.high - p0.low) >= 0.65 && p0.close > p0.open)\n    : false;\n\n  return {\n    close, atr14: t0.atr14,\n    ema9: t0.ema9, ema20: t0.ema20, ema50: t0.ema50, ema200: t0.ema200,\n    ema50Rising,\n    rsi14: t0.rsi14, prevRsi14: t1.rsi14,\n    macdHistogram: t0.macd_histogram, prevMacdHistogram: t1.macd_histogram,\n    macdBullishCrossover, macdImproving,\n    relativeVolume20: t0.relative_volume20,\n    high20: t0.high20, distance52wHigh: t0.distance_52w_high,\n    return1d, return3d, return5d, return10d, return20d,\n    higherHighs, higherLows, sellVolumeDeclining, bullishCandle,\n    resistance1: sr.resistance1, resistance2: sr.resistance2, resistance3: sr.resistance3,\n    support1: sr.support1, support2: sr.support2, support3: sr.support3,\n    support1Strength: sr.support1_strength,\n    nearestSupportDistancePct: sr.nearest_support_distance_pct,\n    nearestResistanceDistancePct: sr.nearest_resistance_distance_pct,\n  };\n}\n\n/**\n * helpers.js\n *\n * Shared low-level utilities used by every other module in /code and by the\n * equivalent n8n Code nodes. Pure functions only — no I/O, no n8n APIs — so\n * this file can be unit tested with plain `node` and pasted into a Code node\n * body unchanged (just drop the `module.exports` line).\n *\n * Candle shape (normalized, provider-agnostic — see docs/DATA_PROVIDER.md):\n * {\n *   symbol: \"COMI\",\n *   date: \"2026-08-18\",     // ISO date, trading session date\n *   open: 100.20,\n *   high: 103.10,\n *   low: 99.80,\n *   close: 102.70,\n *   volume: 12500000,\n *   tradedValue: 1270000000 // optional, derived if absent\n * }\n */\n\n/** Returns true if v is a finite, non-NaN number. */\nfunction isNumber(v) {\n  return typeof v === \"number\" && Number.isFinite(v);\n}\n\n/** Safe division: returns null instead of Infinity/NaN when denominator is unusable. */\nfunction safeDivide(numerator, denominator) {\n  if (!isNumber(numerator) || !isNumber(denominator) || denominator === 0) {\n    return null;\n  }\n  const result = numerator / denominator;\n  return Number.isFinite(result) ? result : null;\n}\n\n/** Rounds to `decimals` places, returns null for non-numeric input. */\nfunction round(value, decimals = 4) {\n  if (!isNumber(value)) return null;\n  const factor = Math.pow(10, decimals);\n  return Math.round(value * factor) / factor;\n}\n\n/** Percentage change from `from` to `to`. Returns null if from <= 0 or invalid. */\nfunction pctChange(from, to) {\n  if (!isNumber(from) || !isNumber(to) || from === 0) return null;\n  return ((to - from) / from) * 100;\n}\n\n/**\n * Validates a single raw candle. Returns { valid, errors[] }.\n * Mirrors the rules in spec section 31.\n */\nfunction validateCandle(candle) {\n  const errors = [];\n  if (!candle || typeof candle !== \"object\") {\n    return { valid: false, errors: [\"candle is not an object\"] };\n  }\n  const { symbol, date, open, high, low, close, volume } = candle;\n\n  if (!symbol || typeof symbol !== \"string\") errors.push(\"missing/invalid symbol\");\n  if (!date || Number.isNaN(Date.parse(date))) errors.push(\"missing/invalid date\");\n  if (!isNumber(open)) errors.push(\"open is not numeric\");\n  if (!isNumber(high)) errors.push(\"high is not numeric\");\n  if (!isNumber(low)) errors.push(\"low is not numeric\");\n  if (!isNumber(close)) errors.push(\"close is not numeric\");\n  if (!isNumber(volume)) errors.push(\"volume is not numeric\");\n\n  if (isNumber(open) && isNumber(high) && isNumber(low) && isNumber(close)) {\n    if (high < open) errors.push(\"high < open\");\n    if (high < close) errors.push(\"high < close\");\n    if (high < low) errors.push(\"high < low\");\n    if (low > open) errors.push(\"low > open\");\n    if (low > close) errors.push(\"low > close\");\n  }\n  if (isNumber(volume) && volume < 0) errors.push(\"negative volume\");\n  if (isNumber(close) && close <= 0) errors.push(\"close <= 0\");\n\n  return { valid: errors.length === 0, errors };\n}\n\n/**\n * Filters an array of raw candles into { valid[], invalid[] }.\n * `invalid` entries carry the original candle plus `.errors`.\n * Zero-volume candles are valid but flagged with a warning for downstream\n * consumers (suspicious, not necessarily wrong — EGX thin names do trade zero\n * volume on quiet sessions).\n */\nfunction validateCandles(candles) {\n  const valid = [];\n  const invalid = [];\n  for (const candle of candles || []) {\n    const { valid: ok, errors } = validateCandle(candle);\n    if (ok) {\n      valid.push({ ...candle, suspiciousZeroVolume: candle.volume === 0 });\n    } else {\n      invalid.push({ ...candle, errors });\n    }\n  }\n  return { valid, invalid };\n}\n\n/**\n * Removes duplicate candles (same symbol+date), keeping the last occurrence\n * (assumed most recent fetch / most authoritative).\n */\nfunction dedupeCandles(candles) {\n  const map = new Map();\n  for (const c of candles || []) {\n    map.set(`${c.symbol}::${c.date}`, c);\n  }\n  return Array.from(map.values());\n}\n\n/** Sorts candles ascending by date (oldest first) — mandatory before any TA calc. */\nfunction sortCandlesAscending(candles) {\n  return [...(candles || [])].sort((a, b) => new Date(a.date) - new Date(b.date));\n}\n\n/**\n * Derives tradedValue when the provider doesn't supply it directly.\n * Uses the session's typical price (H+L+C)/3 * volume as a reasonable proxy.\n */\nfunction deriveTradedValue(candle) {\n  if (isNumber(candle.tradedValue)) return candle.tradedValue;\n  if (!isNumber(candle.high) || !isNumber(candle.low) || !isNumber(candle.close) || !isNumber(candle.volume)) {\n    return null;\n  }\n  const typicalPrice = (candle.high + candle.low + candle.close) / 3;\n  return typicalPrice * candle.volume;\n}\n\n/** Simple mean of the last `n` numeric values in `arr` (nulls skipped). Returns null if insufficient data. */\nfunction meanOfLast(arr, n) {\n  if (!Array.isArray(arr) || arr.length < n) return null;\n  const slice = arr.slice(-n).filter(isNumber);\n  if (slice.length < n) return null;\n  return slice.reduce((a, b) => a + b, 0) / n;\n}\n\n/** Standard deviation of the last `n` numeric values. Returns null if insufficient data. */\nfunction stdDevOfLast(arr, n) {\n  const mean = meanOfLast(arr, n);\n  if (mean === null) return null;\n  const slice = arr.slice(-n);\n  const variance = slice.reduce((sum, v) => sum + Math.pow(v - mean, 2), 0) / n;\n  return Math.sqrt(variance);\n}\n\n/** Clamps a number into [min, max]. */\nfunction clamp(value, min, max) {\n  if (!isNumber(value)) return min;\n  return Math.min(Math.max(value, min), max);\n}\n\n/**\n * Wraps arbitrary rows into n8n's expected item structure: [{ json: {...} }].\n * Accepts a single object or array of objects.\n */\nfunction toN8nItems(rows) {\n  const list = Array.isArray(rows) ? rows : [rows];\n  return list.map((json) => ({ json }));\n}\n\n/**\n * Reads all `json` payloads out of an n8n items array back into plain objects.\n */\nfunction fromN8nItems(items) {\n  return (items || []).map((item) => item.json);\n}\n\n// ---------------------------------------------------------------------\n\n/**\n * breakoutScore.js\n *\n * Independent 0-100 breakout score (spec section 14). Input is a single\n * stock's latest technicals + support/resistance snapshot; output is a\n * score plus a human-readable reasons/warnings trail and a breakout\n * classification (BREAKOUT_CONFIRMED / BREAKOUT_WATCH / NO_BREAKOUT).\n */\n\n\n/**\n * input: {\n *   close, atr14, resistance1,\n *   ema9, ema20, ema50,\n *   rsi14, macdHistogram, macdBullishCrossover,\n *   relativeVolume20, high20,\n *   breakoutMinRvol, breakoutStrongRvol, breakoutDistancePct  // env-configurable thresholds\n * }\n */\nfunction calculateBreakoutScore(input) {\n  const {\n    close, atr14, resistance1,\n    ema9, ema20, ema50,\n    rsi14, macdHistogram, macdBullishCrossover,\n    relativeVolume20, high20,\n    breakoutMinRvol = 1.5, breakoutStrongRvol = 2, breakoutDistancePct = 1.5,\n  } = input;\n\n  const reasons = [];\n  const warnings = [];\n  let score = 0;\n\n  if (!isNumber(close)) {\n    return { score: 0, classification: \"NO_BREAKOUT\", reasons, warnings: [\"insufficient price data\"] };\n  }\n\n  // --- Proximity to / breach of resistance (up to 25 pts) ------------------\n  let distancePct = null;\n  if (isNumber(resistance1)) {\n    const distanceRatio = safeDivide(resistance1 - close, close);\n    distancePct = distanceRatio !== null ? distanceRatio * 100 : null;\n  }\n  if (distancePct !== null) {\n    if (distancePct <= 0) {\n      score += 25;\n      reasons.push({ factor: \"resistance\", scoreContribution: 25, message: `Price has broken above resistance (${round(resistance1, 4)})` });\n    } else if (distancePct <= breakoutDistancePct) {\n      const contribution = round(25 * (1 - distancePct / breakoutDistancePct), 2);\n      score += contribution;\n      reasons.push({ factor: \"resistance\", scoreContribution: contribution, message: `Price is ${round(distancePct, 2)}% below resistance (${round(resistance1, 4)})` });\n    }\n  }\n\n  // --- Relative volume (up to 25 pts) --------------------------------------\n  if (isNumber(relativeVolume20)) {\n    if (relativeVolume20 >= breakoutStrongRvol) {\n      score += 25;\n      reasons.push({ factor: \"relative_volume\", value: relativeVolume20, scoreContribution: 25, message: `Relative volume is ${round(relativeVolume20, 2)}x the previous 20-session average` });\n    } else if (relativeVolume20 >= breakoutMinRvol) {\n      // Reaching this branch requires breakoutMinRvol <= relativeVolume20 < breakoutStrongRvol,\n      // which is only possible when breakoutStrongRvol > breakoutMinRvol — so the\n      // denominator below is always positive whenever this line executes.\n      const contribution = round(15 + 10 * ((relativeVolume20 - breakoutMinRvol) / (breakoutStrongRvol - breakoutMinRvol)), 2);\n      score += contribution;\n      reasons.push({ factor: \"relative_volume\", value: relativeVolume20, scoreContribution: contribution, message: `Relative volume is ${round(relativeVolume20, 2)}x, above the ${breakoutMinRvol}x breakout threshold` });\n    } else {\n      warnings.push(`Relative volume ${round(relativeVolume20, 2)}x is below the ${breakoutMinRvol}x breakout threshold`);\n    }\n  }\n\n  // --- Trend structure (up to 20 pts) ---------------------------------------\n  if (isNumber(close) && isNumber(ema9) && isNumber(ema20) && isNumber(ema50)) {\n    if (close > ema9 && ema9 > ema20 && ema20 > ema50) {\n      score += 20;\n      reasons.push({ factor: \"trend\", scoreContribution: 20, message: \"EMA9 > EMA20 > EMA50 with price above all three\" });\n    } else if (close > ema20) {\n      score += 10;\n      reasons.push({ factor: \"trend\", scoreContribution: 10, message: \"Price above EMA20 but full EMA stack not yet aligned\" });\n    }\n  }\n\n  // --- MACD (up to 10 pts) ---------------------------------------------------\n  if (macdBullishCrossover) {\n    score += 10;\n    reasons.push({ factor: \"macd\", scoreContribution: 10, message: \"MACD bullish crossover\" });\n  } else if (isNumber(macdHistogram) && macdHistogram > 0) {\n    score += 5;\n    reasons.push({ factor: \"macd\", scoreContribution: 5, message: \"MACD histogram positive\" });\n  }\n\n  // --- RSI (up to 10 pts), interpreted per section 12 ------------------------\n  if (isNumber(rsi14)) {\n    if (rsi14 >= 55 && rsi14 <= 70) {\n      score += 10;\n      reasons.push({ factor: \"rsi\", value: rsi14, scoreContribution: 10, message: `RSI ${round(rsi14, 1)} supports a breakout without being overextended` });\n    } else if (rsi14 > 70 && rsi14 <= 80) {\n      score += 4;\n      warnings.push(`RSI ${round(rsi14, 1)} is elevated — reduced score applied`);\n    } else if (rsi14 > 80) {\n      warnings.push(`RSI ${round(rsi14, 1)} is overextended (>80)`);\n    } else if (rsi14 >= 45) {\n      score += 5;\n    }\n  }\n\n  // --- Recent-highs confirmation (up to 10 pts) ------------------------------\n  if (isNumber(high20) && close >= high20 * 0.995) {\n    score += 10;\n    reasons.push({ factor: \"recent_highs\", scoreContribution: 10, message: \"Price is at or near its 20-session high\" });\n  }\n\n  score = clamp(round(score, 2), 0, 100);\n\n  // --- Classification ---------------------------------------------------------\n  let classification = \"NO_BREAKOUT\";\n  if (isNumber(resistance1) && isNumber(atr14)) {\n    const buffer = Math.max(atr14 * 0.25, close * 0.003); // ATR or 0.3% buffer, whichever larger\n    if (close > resistance1 + buffer) {\n      classification = \"BREAKOUT_CONFIRMED\";\n    } else if (distancePct !== null && distancePct <= breakoutDistancePct) {\n      classification = \"BREAKOUT_WATCH\";\n    }\n  } else if (distancePct !== null && distancePct <= 0) {\n    classification = \"BREAKOUT_CONFIRMED\";\n  } else if (distancePct !== null && distancePct <= breakoutDistancePct) {\n    classification = \"BREAKOUT_WATCH\";\n  }\n\n  if (classification === \"NO_BREAKOUT\" && !isNumber(resistance1)) {\n    warnings.push(\"No resistance level available yet — classification defaults to NO_BREAKOUT\");\n  }\n\n  return { score, classification, reasons, warnings };\n}\n\n// Runs once for the WHOLE batch — one item per active stock arrives here\n// from \"Load Snapshot\". Must loop $input.all() explicitly (confirmed as a\n// real bug via live E2E testing: $input.first() silently only scored the\n// first stock in the list). itemMatching(i) pairs each row back to its stock.\n\nfunction scoreOneStock(row, derived) {\n  if (!isNumber(derived.close)) {\n    return { score: 0, reasons: [], warnings: ['No price data available for this stock on the scan date.'] };\n  }\n  const result = calculateBreakoutScore({\n  close: derived.close, atr14: derived.atr14, resistance1: derived.resistance1,\n  ema9: derived.ema9, ema20: derived.ema20, ema50: derived.ema50,\n  rsi14: derived.rsi14, macdHistogram: derived.macdHistogram, macdBullishCrossover: derived.macdBullishCrossover,\n  relativeVolume20: derived.relativeVolume20, high20: derived.high20,\n  breakoutMinRvol: Number($env.BREAKOUT_MIN_RVOL) || 1.5,\n  breakoutStrongRvol: Number($env.BREAKOUT_STRONG_RVOL) || 2,\n  breakoutDistancePct: Number($env.BREAKOUT_DISTANCE_PCT) || 1.5,\n});\n  const reasons = result.reasons || [];\n  if (result.classification) {\n    reasons.unshift({ factor: 'classification', scoreContribution: 0, message: 'Classification: ' + result.classification });\n  }\n  return { score: result.score, reasons, warnings: result.warnings || [] };\n}\n\nconst items = $input.all();\nconst out = [];\n\nfor (let i = 0; i < items.length; i++) {\n  let original;\n  try { original = $('Attach Scanner Context').itemMatching(i).json; } catch (e) { continue; }\n  const stockId = original.id;\n  const symbol = original.symbol;\n  const scannerRunId = original.scannerRunId;\n\n  const row = items[i].json;\n  const derived = deriveFields(row);\n  const scored = scoreOneStock(row, derived);\n\n  out.push({ json: { stockId, scannerRunId, symbol, ...scored } });\n}\n\nreturn out;"
      },
      "position": [
        1820,
        0
      ],
      "id": "3a09897e-a8f3-4574-abe1-555341a48868"
    },
    {
      "name": "Upsert Scanner Result",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "parameters": {
        "operation": "executeQuery",
        "query": "INSERT INTO scanner_results (scanner_run_id, stock_id, breakout_score, reasons_json, warnings_json)\nVALUES ($1, $2, $3, $4::jsonb, $5::jsonb)\nON CONFLICT (scanner_run_id, stock_id) DO UPDATE SET\n  breakout_score = EXCLUDED.breakout_score,\n  reasons_json = COALESCE(scanner_results.reasons_json, '[]'::jsonb) || EXCLUDED.reasons_json,\n  warnings_json = COALESCE(scanner_results.warnings_json, '[]'::jsonb) || EXCLUDED.warnings_json;",
        "options": {
          "queryReplacement": "={{ [$json.scannerRunId, $json.stockId, $json.score, JSON.stringify($json.reasons), JSON.stringify($json.warnings)] }}"
        }
      },
      "onError": "continueRegularOutput",
      "position": [
        2080,
        0
      ],
      "id": "70b4d541-b434-400b-8c00-852508dc288e"
    },
    {
      "name": "Count Scored Stocks",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT COUNT(*)::int AS scored_count FROM scanner_results WHERE scanner_run_id = $1 AND breakout_score > 0;",
        "options": {
          "queryReplacement": "={{ [$('Get or Create Scanner Run').first().json.id] }}"
        }
      },
      "executeOnce": true,
      "position": [
        2340,
        0
      ],
      "id": "6720630c-21b2-4928-b092-89d52af997a2"
    },
    {
      "name": "Build Summary",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "const count = $input.first().json.scored_count;\nreturn [{ json: {\n  workflow: '07-egx-breakout-scanner',\n  finishedAt: new Date().toISOString(),\n  scannerRunId: $('Get or Create Scanner Run').first().json.id,\n  breakoutPositiveScoreCount: count,\n} }];"
      },
      "position": [
        2600,
        0
      ],
      "id": "0283b759-1bfa-4549-b9bf-23d408884bf3"
    }
  ],
  "connections": {
    "Manual Trigger": {
      "main": [
        [
          {
            "node": "Attach As-Of Date",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "When Executed by Another Workflow": {
      "main": [
        [
          {
            "node": "Attach As-Of Date",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Attach As-Of Date": {
      "main": [
        [
          {
            "node": "Get or Create Scanner Run",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get or Create Scanner Run": {
      "main": [
        [
          {
            "node": "Load Active Stocks",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Load Active Stocks": {
      "main": [
        [
          {
            "node": "Attach Scanner Context",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Attach Scanner Context": {
      "main": [
        [
          {
            "node": "Load Snapshot",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Load Snapshot": {
      "main": [
        [
          {
            "node": "Calculate Breakout Score",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Calculate Breakout Score": {
      "main": [
        [
          {
            "node": "Upsert Scanner Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Upsert Scanner Result": {
      "main": [
        [
          {
            "node": "Count Scored Stocks",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Count Scored Stocks": {
      "main": [
        [
          {
            "node": "Build Summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "pinData": {},
  "settings": {
    "executionOrder": "v1"
  },
  "staticData": null,
  "meta": {
    "templateCredsSetupCompleted": false
  },
  "id": "5df05f05-5608-4b8b-b260-63f0df2e6105",
  "tags": []
}
