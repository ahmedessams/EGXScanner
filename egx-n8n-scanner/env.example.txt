# =====================================================================
# EGX N8N Scanner - environment configuration
# Copy this file to ".env" (rename, drop the .txt) and fill in real values.
# NEVER commit the real .env file.
# =====================================================================

# --- General -----------------------------------------------------------
TZ=Africa/Cairo

# --- n8n -----------------------------------------------------------------
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http
WEBHOOK_URL=http://localhost:5678/

# --- Dashboard web app (webapp/index.html, served by nginx) -----------------
# Static, dependency-free dashboard over the Report API. Configure its API
# base URL / bearer token from within the app (gear icon) after it loads.
WEBAPP_PORT=8090
# Generate with: openssl rand -hex 32
N8N_ENCRYPTION_KEY=
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=

# --- PostgreSQL ------------------------------------------------------------
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=egx_scanner
POSTGRES_USER=egx_user
POSTGRES_PASSWORD=

# --- Market data provider abstraction ---------------------------------------
# See docs/DATA_PROVIDER.md for the normalization contract and how to add
# a new provider. Do NOT hardcode a provider name anywhere else.
MARKET_DATA_PROVIDER=eodhd
MARKET_API_BASE_URL=
MARKET_API_KEY=
EGX_EXCHANGE_CODE=EGX

# --- AI Assessment (workflow 17, optional) -----------------------------------
# If left blank, 17-egx-ai-assessment simply produces no AI columns (they
# stay NULL) rather than failing — the rest of the pipeline is unaffected.
ANTHROPIC_API_KEY=

# --- Historical import ----------------------------------------------------
# Target number of trading sessions to backfill (workflow 02).
HISTORICAL_DAYS=750
# Batch size and inter-batch delay for rate-limited provider calls.
API_BATCH_SIZE=10
API_DELAY_MS=500

# --- Liquidity filter (section 19) ------------------------------------------
# A stock must clear these thresholds (20-session averages) to be TOP-N
# eligible. Stocks below threshold still appear in the full market table,
# marked NOT_ELIGIBLE.
MIN_AVG_TRADED_VALUE=500000
MIN_AVG_VOLUME=50000
MIN_ACTIVE_DAYS_20=15

# --- Daily schedule ----------------------------------------------------------
# EGX regular session closes ~14:30 Cairo time; default scan time gives a
# buffer for the data provider to publish end-of-day data.
DAILY_SCAN_HOUR=15
DAILY_SCAN_MINUTE=30

# --- Ranking ----------------------------------------------------------------
TOP_N=10

# --- Breakout scanner thresholds (section 14) --------------------------------
BREAKOUT_MIN_RVOL=1.5
BREAKOUT_STRONG_RVOL=2
BREAKOUT_DISTANCE_PCT=1.5

# --- Support / resistance engine (section 10) ---------------------------------
SUPPORT_RESISTANCE_LOOKBACK=150

# --- Report API security (section 38) -----------------------------------------
# Required Bearer token for action-triggering / expensive webhook endpoints.
# Generate with: openssl rand -hex 32
REPORT_WEBHOOK_SECRET=
